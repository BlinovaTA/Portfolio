.works-wrapper {
  display: flex;
  flex-direction: column;
  height: 100%;
}

.work__pic {
  height: 190px;
  position: relative;
}

.work__tag {
  position: absolute;
  bottom: 2px;
  right: 0;
}

.work__image {
  object-fit: cover;
  height: 100%;
  width: 100%;
}

.work__data {
  padding: 40px 30px;
  flex: 1;
  display: flex;
  flex-direction: column;
}

.work__title {
  font-size: 18px;
  font-weight: bold;
  margin-bottom: 15px;
}

.work__text {
  font-weight: 600;
  line-height: 1.88;
  color: rgba(65, 76, 99, 0.5);
  margin-bottom: 20px;
}

.work__link {
  display: block;
  font-weight: 600;
  color: #383bcf;
  margin-bottom: 44px;
  margin-top: auto;
}

.work__btns {
  display: flex;
  justify-content: space-between;
}
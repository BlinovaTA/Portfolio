.round-btn {
  height: 53px;
  width: 53px;
  border-radius: 50%;
  border: 2px solid $text-color;
  background: transparent;
  margin-right: 28px;

  &:focus {
    outline: none;
  }

  &:last-child {
    margin-right: 0;
  }

  &--pointer--left {
    .round-btn__icon {
      transform: rotate(180deg);
    }
  }

  &__icon {
    height: 18px;
    width: 18px;
    fill: $text-color;
  }
}
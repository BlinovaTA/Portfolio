.menu {
  &__list {
    display: flex;
    justify-content: center;
  }

  &__item {
    margin-right: 60px;

    &:last-child {
      margin-right: 0;
    }
  }

  &__link {
    text-decoration: none;
    white-space: nowrap;
  }

  &--color-white {
    color: white;
  }
}